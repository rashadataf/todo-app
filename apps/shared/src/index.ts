import { ConfigKey, NodeEnvironment } from './enums';
import { DatabaseConfig, ServerConfig } from './types';

export type {
    DatabaseConfig,
    ServerConfig,
};

export {
    ConfigKey,
    NodeEnvironment
}